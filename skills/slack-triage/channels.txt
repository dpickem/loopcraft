# Slack channels to triage — PUBLIC, committed.
#
# This file holds only non-confidential placeholders. Real channel lists are
# private and must NOT be committed here. Provide them via either (highest
# precedence first):
#
#   1. env var  LOOPCRAFT_SLACK_TRIAGE_CHANNELS  (comma- or newline-separated;
#      put it in .env, which is gitignored)
#   2. a gitignored sibling file  channels.local.txt  (one channel per line)
#   3. this public file (placeholders below)
#
# At run time the control plane resolves the effective list and writes it into
# the isolated run worktree, so confidential channels stay out of the repo.
#
# Uncomment/add a channel below ONLY if its name is safe to commit:
# team-announcements
# incidents
